name: Run tests

on:
  pull_request:
    branches: [ "main", "dev" ]

jobs:
  test:
    runs-on: self-hosted

    strategy:
      matrix:
        maven: ['3.6.3']

    steps:
    - uses: actions/checkout@v4
    - uses: s4u/setup-maven-action@v1.19.0
      with:
        java-version: 17
        maven-version: ${{ matrix.maven }}
    - uses: actions/setup-node@v5
      with:
        node-version: 22
    - uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    
    - name: Starting postgres for backend build
      run: cd test-db && docker compose up -d
      
    - name: Running maven build
      run: cd backend && mvn clean package
      
    - name: Stopping postgres
      run: cd test-db && docker compose down
      
    - name: Building frontend
      run: cd frontend && npm i && npm run build
      
    - name: Build docker image for backend
      run: cd backend && docker build . -t "prinshertog/kokenmetlisa-backend:v1-unstable"
      
    - name: Build docker image for frontend
      run: cd frontend && docker build . -t "prinshertog/kokenmetlisa-frontend:v1-unstable"
